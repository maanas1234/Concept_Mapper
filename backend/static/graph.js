// Placeholder for tomorrow's 3D graph rendering
window.renderGraph = function renderGraph(graphData) {
    // For now we just log; the actual Three.js integration will live here later.
    console.log("Graph data received:", graphData);
};
